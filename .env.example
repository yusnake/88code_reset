# 88code 订阅重置工具配置文件示例
# 复制此文件为 .env 并修改配置

# ========================================
# 单账号模式配置
# ========================================

# API Key（单账号模式）
# 用于 -mode=test, -mode=run, -mode=manual
API_KEY=your_api_key_here
# 或使用旧格式
# api-key=your_api_key_here

# ========================================
# 多账号模式配置
# ========================================

# 多个 API Keys（多账号模式）
# 用于 -mode=import, -mode=multi
# 多个 API Key 用逗号分隔，系统会自动通过 employeeEmail 去重
# API_KEYS=88_key1,88_key2,88_key3
# 或使用旧格式
# api-keys=88_key1,88_key2,88_key3

# ========================================
# 通用配置
# ========================================

# API Base URL（可选，默认为 https://www.88code.org）
# BASE_URL=https://www.88code.org

# 时区设置（可选，默认为 Asia/Shanghai UTC+8）
# 支持标准时区名称，用于控制重置时间
# 示例：
# TZ=Asia/Shanghai          # 北京/上海时区 (UTC+8)
# TZ=Asia/Hong_Kong         # 香港时区 (UTC+8)
# TZ=Asia/Tokyo             # 东京时区 (UTC+9)
# TZ=America/New_York       # 纽约时区 (UTC-5/-4)
# TZ=Europe/London          # 伦敦时区 (UTC+0/+1)
# TZ=UTC                    # 世界协调时
#
# 或使用 TIMEZONE 变量
# TIMEZONE=Asia/Shanghai
#
# 默认重置时间为北京时间：
# - 第一次: 18:50
# - 第二次: 23:55

# 目标订阅计划（可选，默认为 FREE）
# 支持多个计划，用逗号分隔
# 示例：
# PLANS=FREE
# PLANS=FREE,PRO
# PLANS=PLUS
#
# 注意：PAYGO 类型订阅会自动被过滤，永不重置
